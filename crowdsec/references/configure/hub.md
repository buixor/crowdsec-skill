# Configure — Hub management

Canonical docs: <https://docs.crowdsec.net/docs/next/getting_started/post_installation/console_hub> · `cscli hub` reference <https://docs.crowdsec.net/docs/next/cscli/cscli_hub>

> STUB. To cover:
> - `cscli hub update / upgrade / list`
> - Installing collections / parsers / scenarios / postoverflows
> - `_custom/` overrides — the right way to tweak hub items
> - Pinning to a hub branch / specific version
> - Dependency resolution (collection pulls parsers + scenarios)
> - Hard don't: editing hub-managed files in place; use overrides
